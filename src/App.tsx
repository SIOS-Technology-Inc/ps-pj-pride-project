import { BrowserRouter } from 'react-router-dom';

import { RouterConfig } from './router/RouterConfig';

function App() {
  return (
    <BrowserRouter>
      <RouterConfig />
    </BrowserRouter>
  );
}

export default App;
