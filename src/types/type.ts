export type Test = {
  string: string;
};

export type UserDataType = {
  id: number;
  name: string;
  gender: number;
  mail: string;
};

export type testType = {
  id: number;
  text: string;
  done: boolean;
};
